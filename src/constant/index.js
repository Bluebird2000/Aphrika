import images from './images';

import theme, { COLORS, SIZES, FONTS } from "./theme";

import { slides, pendingResult, usersList } from "./asset"


export { images, theme, COLORS, SIZES, FONTS, slides, pendingResult, usersList };